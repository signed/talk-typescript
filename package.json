{
  "name": "talk-typescript",
  "description": "talk about nice features in typescript",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "test": "vitest --run",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "nit-pick": "pnpm format && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@signed/prettier-config": "0.1.0",
    "prettier": "3.0.2",
    "ts-node": "10.9.1",
    "typescript": "5.2.2",
    "vitest": "0.34.3"
  },
  "prettier": "@signed/prettier-config"
}
